pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                // Example: sh 'pip install -r requirements.txt'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing...'
                // Example: sh 'pytest tests/'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying...'
                // Example: Deploy steps if required
            }
        }
    }
}
